<Window x:Class="CRM.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM"
        xmlns:vm="clr-namespace:CRM.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Клиенты" Background="#99FF99">
                <Grid Background="#99FF99">

                    <Button Content="Добавить" Command="{Binding AddNewClientCommand}" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top"/>
                    <Button Content="Удалить"  Command="{Binding DeleteClientCommand}" HorizontalAlignment="Left" Margin="74,0,0,0" VerticalAlignment="Top"/>

                    <Button Content="Загрузить" Command="{Binding LoadDatabaseCommand}" HorizontalAlignment="Left" Margin="716,0,0,0" VerticalAlignment="Top"/>

                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Clients, Mode=TwoWay}" Margin="0,24,0,0" Grid.ColumnSpan="2">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Nickname" Binding="{Binding Path=Nickname}" />
                            <DataGridTextColumn Header="Phone Number" Binding="{Binding Path=Phone}" />
                            <DataGridTextColumn Header="City" Binding="{Binding Path=City}" />
                            <DataGridTextColumn Header="Shipping Method" Binding="{Binding Path=ShippingMethod}" />
                            <DataGridTextColumn Header="Postal Code" Binding="{Binding Path=PostalCode}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Заказы" Background="#FFCC66">
                <Grid Background="#FFCC66">

                    <Button Content="Добавить" Command="{Binding AddNewClientCommand}" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top"/>
                    <Button Content="Удалить"  Command="{Binding DeleteClientCommand}" HorizontalAlignment="Left" Margin="74,0,0,0" VerticalAlignment="Top"/>

                    <Button Content="Загрузить" Command="{Binding LoadDatabaseCommand}" HorizontalAlignment="Left" Margin="716,0,0,0" VerticalAlignment="Top"/>

                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Clients, Mode=TwoWay}" Margin="0,24,0,0" Grid.ColumnSpan="2">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Nickname" Binding="{Binding Path=Nickname}" />
                            <DataGridTextColumn Header="Phone Number" Binding="{Binding Path=Phone}" />
                            <DataGridTextColumn Header="City" Binding="{Binding Path=City}" />
                            <DataGridTextColumn Header="Shipping Method" Binding="{Binding Path=ShippingMethod}" />
                            <DataGridTextColumn Header="Postal Code" Binding="{Binding Path=PostalCode}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Склад">
                <Grid Background="#FFE5E5E5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="15*"/>
                        <ColumnDefinition Width="779*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Добавить" Command="{Binding AddNewItemCommand}" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Grid.ColumnSpan="2"/>
                    <Button Content="Обновить" HorizontalAlignment="Left" Margin="124,0,0,0" VerticalAlignment="Top" Grid.Column="1"/>
                    <Button Content="Удалить" HorizontalAlignment="Left" Margin="59,0,0,0" VerticalAlignment="Top" Grid.Column="1"/>

                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Stock}" Margin="0,24,0,0" Grid.ColumnSpan="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Title" Binding="{Binding Path=Title}" />
                            <DataGridTextColumn Header="Manufacturer" Binding="{Binding Path=Manufacturer}" />
                            <DataGridTextColumn Header="Purchase Price" Binding="{Binding Path=PurchasePrice}" />
                            <DataGridTextColumn Header="Retail Price" Binding="{Binding Path=RetailPrice}" />
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Path=Quantity}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>


        <!--<ListView x:Name="OrdersList" Margin="0,22,-0.4,0" Grid.Column="12" HorizontalAlignment="Left"/>-->
    </Grid>
</Window>

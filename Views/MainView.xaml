<Window x:Class="CRM.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM"
        xmlns:vm="clr-namespace:CRM.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="1200">
        
    <Grid Margin="10">

        <!--<Grid.RowDefinitions>
            <RowDefinition Height="0.16*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.30*"></ColumnDefinition>
            <ColumnDefinition Width="0.30*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="0.30*"></ColumnDefinition>
        </Grid.ColumnDefinitions>-->

        <TabControl>
            <TabItem Header="Клиенты" Background="#99FF99">
                <Grid Background="#99FF99">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Button Content="Добавить" Command="{Binding AddNewClientCommand}" Grid.Column="0" Margin="5" BorderThickness="1"/>
                    <Button Content="Удалить"  Command="{Binding DeleteClientCommand}" Grid.Column="1" Margin="5" BorderThickness="1"/>

                    <Button Content="Загрузить" Command="{Binding LoadDatabaseCommand}" Grid.Column="3" Margin="5" BorderThickness="1"/>

                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Clients, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Nickname" Binding="{Binding Path=Nickname}" />
                            <DataGridTextColumn Header="Phone Number" Binding="{Binding Path=Phone}" />
                            <DataGridTextColumn Header="City" Binding="{Binding Path=City}" />
                            <DataGridTextColumn Header="Shipping Method" Binding="{Binding Path=ShippingMethod}" />
                            <DataGridTextColumn Header="Postal Code" Binding="{Binding Path=PostalCode}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Заказы" Background="#FFCC66">
                <Grid Background="#FFCC66">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Button Content="Добавить" Command="{Binding AddNewOrderCommand}" Grid.Column="0" Margin="5" BorderThickness="1"/>
                    <Button Content="Удалить"  Command="{Binding DeleteOrderCommand}" Grid.Column="1" Margin="5" BorderThickness="1"/>

                    <Button Content="Загрузить" Command="{Binding LoadOrdersCommand}" Grid.Column="3" Margin="5" BorderThickness="1"/>

                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Orders, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Client" Binding="{Binding Path=Client.Name}" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Path=Status}" />
                            <DataGridTextColumn Header="Total" Binding="{Binding Path=Total}" />
                            <DataGridTextColumn Header="Notes" Binding="{Binding Path=Notes}" />
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem Header="Склад">
                <Grid Background="#FFE5E5E5">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Button Content="Добавить" Command="{Binding AddNewItemCommand}" Grid.Column="0" Margin="5" BorderThickness="1"/>
                    <Button Content="Обновить" Grid.Column="1" Margin="5" BorderThickness="1"/>
                    <Button Content="Удалить" Grid.Column="3" Margin="5" BorderThickness="1"/>

                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Stock}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Title" Binding="{Binding Path=Title}" />
                            <DataGridTextColumn Header="Manufacturer" Binding="{Binding Path=Manufacturer}" />
                            <DataGridTextColumn Header="Purchase Price" Binding="{Binding Path=PurchasePrice}" />
                            <DataGridTextColumn Header="Retail Price" Binding="{Binding Path=RetailPrice}" />
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Path=Quantity}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Курс валют">
                <Grid Background="#FFE5E5E5">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*" Name="col3"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    
                    <Button Content="Удалить" HorizontalAlignment="Left" Margin="289,183,0,0" VerticalAlignment="Top" Grid.Column="1"/>

                    <!--<StackPanel Grid.Row="0" Grid.ColumnSpan="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Добавить" Command="{Binding AddNewItemCommand}" BorderThickness="1" />

                        <TextBlock>Валюта:</TextBlock>
                        <TextBox Grid.Column="1">gdhfhgf</TextBox>
                    </StackPanel>-->

                    <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0">Валюта: </TextBlock>
                    <TextBox Text="{Binding Path=Currency, Mode=TwoWay}" Grid.Row="1" Grid.Column="3" HorizontalAlignment="Stretch" Width="{Binding ElementName=col3, Path=Width}" VerticalAlignment="Center"></TextBox>

                    <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0">Курс: </TextBlock>
                    <TextBox Text="{Binding Path=Rate, Mode=TwoWay}" Grid.Row="2" Grid.Column="3" HorizontalAlignment="Stretch" Width="{Binding ElementName=col3, Path=Width}" VerticalAlignment="Center"></TextBox>

                    <Button Content="Добавить" Command="{Binding AddNewExchangeRateCommand}" BorderThickness="1" Grid.Row="3" Grid.Column="3"/>

                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.ExchangeRates, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="6">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Path=Date}" />
                            <DataGridTextColumn Header="Currency" Binding="{Binding Path=Currency}" />
                            <DataGridTextColumn Header="Rate" Binding="{Binding Path=Rate}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>


        <!--<ListView x:Name="OrdersList" Margin="0,22,-0.4,0" Grid.Column="12" HorizontalAlignment="Left"/>-->
    </Grid>
</Window>

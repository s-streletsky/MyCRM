<Window x:Class="CRM.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:CRM"
        xmlns:models="clr-namespace:CRM.Models"
        xmlns:vm="clr-namespace:CRM.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="1200">

    <Window.Resources>
        <Style x:Key="TopRowButton">
            <Setter Property="Button.Width" Value="80" />
            <Setter Property="Button.Margin" Value="0,0,5,5" />
        </Style>

        <Style x:Key="InnerGrid">
            <Setter Property="Grid.Margin" Value="5" />
        </Style>

        <Style TargetType="TextBlock" x:Key="HeaderTextBlockStyle">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected,
                          RelativeSource={RelativeSource AncestorType=TabItem}}"
                         Value="True">
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">

        <TabControl>
            <TabItem>
                
                <TabItem.Header>
                    <TextBlock Text="Клиенты"
                     Style="{StaticResource HeaderTextBlockStyle}"/>
                </TabItem.Header>

                <Grid Style="{StaticResource InnerGrid}">
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource TopRowButton}" Content="Добавить" Command="{Binding AddNewClientCommand}" />
                        <Button Style="{StaticResource TopRowButton}" Content="Изменить" />
                        <Button Style="{StaticResource TopRowButton}" Content="Удалить"  Command="{Binding DeleteClientCommand}" />
                        <!--<Button Style="{StaticResource TopRowButton}" Content="Загрузить" Command="{Binding LoadDatabaseCommand}" />-->
                    </StackPanel>

                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Clients, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                        
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Nickname" Binding="{Binding Path=Nickname}" />
                            <DataGridTextColumn Header="Phone Number" Binding="{Binding Path=Phone}" />
                            <DataGridTextColumn Header="City" Binding="{Binding Path=City}" />
                            <DataGridTextColumn Header="Shipping Method" Binding="{Binding Path=ShippingMethod}" />
                            <DataGridTextColumn Header="Postal Code" Binding="{Binding Path=PostalCode}" />
                        </DataGrid.Columns>
                        
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <TabItem>

                <TabItem.Header>
                    <TextBlock Text="Заказы"
                     Style="{StaticResource HeaderTextBlockStyle}"/>
                </TabItem.Header>
                
                <Grid Style="{StaticResource InnerGrid}">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource TopRowButton}" Content="Добавить" Command="{Binding AddNewOrderCommand}" />
                        <Button Style="{StaticResource TopRowButton}" Content="Изменить" />
                        <Button Style="{StaticResource TopRowButton}" Content="Удалить"  Command="{Binding DeleteOrderCommand}" />
                        <!--<Button Style="{StaticResource TopRowButton}" Content="Загрузить" Command="{Binding LoadOrdersCommand}" />-->
                    </StackPanel>
                                        
                    <DataGrid SelectedItem="{Binding Path=SelectedClient, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.Orders, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditClientCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Client" Binding="{Binding Path=Client.Name}" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Path=Status}" />
                            <DataGridTextColumn Header="Total" Binding="{Binding Path=Total}" />
                            <DataGridTextColumn Header="Notes" Binding="{Binding Path=Notes}" />
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>

            <TabItem>

                <TabItem.Header>
                    <TextBlock Text="Склад"
                     Style="{StaticResource HeaderTextBlockStyle}"/>
                </TabItem.Header>
                
                <Grid Style="{StaticResource InnerGrid}">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource TopRowButton}" Content="Добавить" Command="{Binding AddNewStockItemCommand}" />
                        <Button Style="{StaticResource TopRowButton}" Content="Изменить" />
                        <Button Style="{StaticResource TopRowButton}" Content="Удалить" />
                    </StackPanel>

                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.StockItems}" Grid.Row="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Manufacturer" Binding="{Binding Path=ManufacturerId}" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Path=Description}" />
                            <DataGridTextColumn Header="Currency" Binding="{Binding Path=Currency}" />
                            <DataGridTextColumn Header="Purchase Price" Binding="{Binding Path=PurchasePrice}" />
                            <DataGridTextColumn Header="Retail Price" Binding="{Binding Path=RetailPrice}" />
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Path=Quantity}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem>

                <TabItem.Header>
                    <TextBlock Text="Курс валют"
                     Style="{StaticResource HeaderTextBlockStyle}"/>
                </TabItem.Header>

                <Grid>
                    <Grid.Resources>
                        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="currencyDataProvider">
                            <ObjectDataProvider.MethodParameters>
                                <x:Type TypeName="models:Currency" />
                            </ObjectDataProvider.MethodParameters>
                        </ObjectDataProvider>
                    </Grid.Resources>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition Height="0.06*"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*"></ColumnDefinition>
                        <ColumnDefinition Width="0.20*" Name="col3"></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition Width="0.15*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>


                    <Button Content="Удалить" HorizontalAlignment="Left" Margin="289,183,0,0" VerticalAlignment="Top" Grid.Column="1"/>

                    <!--<StackPanel Grid.Row="0" Grid.ColumnSpan="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Добавить" Command="{Binding AddNewItemCommand}" BorderThickness="1" />

                        <TextBlock>Валюта:</TextBlock>
                        <TextBox Grid.Column="1">gdhfhgf</TextBox>
                    </StackPanel>-->

                    <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0">Валюта:</TextBlock>
                    <ComboBox ItemsSource="{Binding Source={StaticResource currencyDataProvider}}" SelectedItem="{Binding Path=SelectedCurrency}" Grid.Row="1" Grid.Column="3" HorizontalAlignment="Stretch" Width="{Binding ElementName=col3, Path=Width}" VerticalAlignment="Center"></ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0">Курс:</TextBlock>
                    <TextBox Text="{Binding Path=CurrencyExchangeRate, Mode=TwoWay}" Grid.Row="2" Grid.Column="3" HorizontalAlignment="Stretch" Width="{Binding ElementName=col3, Path=Width}" VerticalAlignment="Center"></TextBox>

                    <Button Content="Добавить" Command="{Binding AddNewExchangeRateCommand}" BorderThickness="1" Grid.Row="3" Grid.Column="3"/>

                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Database.ExchangeRates, Mode=TwoWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="6">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Path=Date}" />
                            <DataGridTextColumn Header="Currency" Binding="{Binding Path=Currency}" />
                            <DataGridTextColumn Header="Rate" Binding="{Binding Path=Rate}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        
    </Grid>
</Window>
